language: android
jdk: oraclejdk8
sudo: required
android:
  components:
    - build-tools-26.0.2
    - android-26
    - extra
    licenses:
    - 'android-sdk-preview-license-52d11cd2'
    - 'android-sdk-license-.+'
    - 'google-gdk-license-.+'
install: true
before_script:
 - chmod +x gradlew
 script:
  - ./gradlew assembleDebug --stacktrace
